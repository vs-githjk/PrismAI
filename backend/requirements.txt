fastapi>=0.111.0,<0.136.0
starlette>=0.46.0,<1.0.0
uvicorn[standard]>=0.30.0
anthropic>=0.40.0
python-dotenv>=1.0.0
httpx[http2]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9
supabase>=2.0.0
# PyJWT: local HS256 verification of Supabase access tokens in auth.py.
# Pinned <3 because the 3.x line will introduce breaking API changes.
PyJWT>=2.10.0,<3
psycopg2-binary>=2.9.0
tzdata>=2024.1
edge-tts>=6.1.0
openai>=1.30.0
pymupdf>=1.24.0
python-docx>=1.1.0
pytesseract>=0.3.10
Pillow>=10.0.0
tiktoken>=0.7.0
notion-client>=2.2.1
google-api-python-client>=2.130.0
google-auth>=2.30.0
# pysbd: rule-based sentence segmenter for streaming TTS dispatch (PR-3+).
# Upstream is feature-complete ("done"), last release 2021. Pinned strict to
# control the supply-chain surface; if a security patch is ever needed we'll
# have to fork or migrate.
pysbd==0.3.4
langgraph==1.2.0
dateparser
# Managed cloud browser: the bot's presentation screen (backend/sandbox/,
# default provider since ADR 0003). Pinned to the spike-verified pair — the
# adapter depends on verified SDK behaviors (project resolved from key, Context
# create + persist bind, keep_alive sessions surviving a CDP disconnect, live
# debug URL). playwright drives the session over CDP (connect_over_cdp);
# `python -m playwright install chromium` provides the local driver. Bump only
# after re-running backend/sandbox/browserbase_provider.py to PASS.
browserbase==1.15.0
playwright==1.61.0
# E2B desktop sandboxes: the FALLBACK presentation provider (PRISM_SANDBOX_PROVIDER=e2b).
# Pinned exactly to the spike-verified pair — the adapter depends on verified
# SDK behaviors (client-side stream auth key, connect() auto-resume, lifecycle
# auto-pause). Bump only after re-running backend/spikes/spike1_e2b_torture.py.
e2b==2.31.0
e2b-desktop==2.4.1
