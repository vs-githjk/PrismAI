fastapi>=0.111.0,<0.136.0
starlette>=0.46.0,<1.0.0
uvicorn[standard]>=0.30.0
anthropic>=0.40.0
python-dotenv>=1.0.0
httpx[http2]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9
supabase>=2.0.0
# PyJWT: local HS256 verification of Supabase access tokens in auth.py.
# Pinned <3 because the 3.x line will introduce breaking API changes.
PyJWT>=2.10.0,<3
psycopg2-binary>=2.9.0
tzdata>=2024.1
edge-tts>=6.1.0
openai>=1.30.0
pymupdf>=1.24.0
python-docx>=1.1.0
pytesseract>=0.3.10
Pillow>=10.0.0
tiktoken>=0.7.0
notion-client>=2.2.1
google-api-python-client>=2.130.0
google-auth>=2.30.0
# pysbd: rule-based sentence segmenter for streaming TTS dispatch (PR-3+).
# Upstream is feature-complete ("done"), last release 2021. Pinned strict to
# control the supply-chain surface; if a security patch is ever needed we'll
# have to fork or migrate.
pysbd==0.3.4
langgraph==1.2.0
dateparser
# groq: re-added Jul 2026 for the voice channel (Phase 3) — a tool-less streaming
# talk brain where Groq's TTFT is the point. Removed Jun 2026 (bef49a3) when the
# free-tier 100k-tok/day cap turned a runaway-analysis bug into an outage; that bug
# is fixed and only the (low-volume) voice channel is on Groq now. SDK is
# OpenAI-API-shaped, so the streaming call pattern is identical.
groq>=0.11.0
# Voice agent (Phase 2): the realtime audio loop — Deepgram Flux ears + Cartesia
# mouth run inside a per-bot Pipecat pipeline (backend/voice/). Pinned to 1.5.0
# (installed + validated; API confirmed via settings=).
#
# [silero] (Phase 5) pulls onnxruntime for the local VAD that owns barge-in — fork ④'s
# "did someone start talking over us?" half. onnxruntime has no Python 3.14 wheels yet;
# render.yaml pins PYTHON_VERSION 3.11.0, so prod gets it. On a 3.14 dev box the install
# fails and the pipeline degrades on purpose (voice/pipeline._make_vad → Flux's own
# interruption + transcript-timestamp politeness gap), so the backend still runs.
pipecat-ai[deepgram,cartesia,silero]==1.5.0
